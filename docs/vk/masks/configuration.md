# Конфигурационный файл

> Источник: [https://dev.vk.ru/ru/masks/configuration](https://dev.vk.ru/ru/masks/configuration)
Конфигурационный файл `mask.json` является входной точкой для запуска маски. В этом файле с помощью языка разметки JSON описываются все необходимые параметры маски.

Так как файл конфигурации представляет из себя объект (в нашем случае объектом является маска), то, согласно синтаксису языка разметки JSON, он должен содержать фигурные&nbsp;скобки&nbsp;`{...}`, внутри которых размещается описание параметров в виде пар `"ключ" : значение`.

```JSON
{
  "icon": "Icon.png",
  "num_faces": 1,
  "gallery_asset_required": false,
  "script": "main.as",
  "user_hint": "open_mouth",
  "mouse_input": true,
  "gestures_required": false,
  "effects": [...],
  "plugins": [...]
}
```

Возможные параметры маски описаны ниже. Каждому параметру соответствует одно поле в JSON-файле.

Мы рекомендуем работать с конфигурационным файлом в текстовом редакторе с подсветкой синтаксиса.

## Параметры

| Параметр | Описание |
| - | - |
| `icon` | Имя файла, используемого для иконки маски в каталоге приложения. Этот файл должен лежать в каталоге с файлом `mask.json` и иметь расширение `.png`. Тип данных: `string`. |
| `num_faces`  | Количество одновременно распознаваемых лиц, на которые рассчитана маска. Допустимые значения:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — детектор лица отключен;&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — детектор ограничивается поиском только одного лица;&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `2` —  детектор ищет два лица.&#x0d;&#x0a;&#x0d;&#x0a;Если в масĸе нет эффеĸтов, завязанных на лице, то для увеличения быстродействия стоит уĸазать `0`. Если не указать это поле, то по умолчанию будет распознано только одно лицо. Подробнее: [Маска на два лица](#Маска%20на%20два%20лица). Тип данных: `integer`. |
| `script` | Имя файла [скрипта](masks/development/script-creation), запускаемого маской. Файл скрипта должен лежать в папке с файлом `mask.json` и называться `main.as`. Расширение `.as` говорит о том, что это файл скриптового языка программирования AngelScript. Тип данных: `string`. |
| `user_hint` | Всплывающая подсказка о действии с маской при её запуске. Если в маске присутствует какой-либо триггер, который меняет её состояние, то необходимо сообщить об этом пользователю. Допустимые значения:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `"open_mouth"` — «Откройте рот»;&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `"tap_change"` — «Коснитесь экрана»;&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `"with_friends"` — «Попробуйте с другом»;&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `start_recording` — «Начните запись».&#x0d;&#x0a;&#x0d;&#x0a;Касания экрана отслеживаются, если для маски указан параметр `"mouse_input": true`.&#x0d;&#x0a;&#x0d;&#x0a;Тип данных: `string`. |
| `mouse_input` | Указывает, отслеживает ли маска касание экрана. Касание может являться триггером событий в [эффектах](masks/effects/overview) или [плагинах](masks/plugins/overview) или обрабатываться в [скриптах](masks/development/script-creation) с помощью события [`MouseEvent`](masks/development/events/MouseEvent).&#x0d;&#x0a;&#x0d;&#x0a;Если `"mouse_input": true`, двойное касание экрана (double tap) не приводит к переключению камер.&#x0d;&#x0a;&#x0d;&#x0a;Тип данных: `boolean`. |
| `gestures_required` | Указывает, распознаёт ли движок масок жесты рук. Для обработки жестов используйте [скрипты](masks/development/script-creation) — создайте обработчик события [`UpdateHandGesture`](masks/development/events/UpdateHandGesture).&#x0d;&#x0a;&#x0d;&#x0a;Тип данных: `boolean`. |
| `gallery_asset_required` | Указывает, будет ли движок масок показывать меню для выбора компонентов маски (`true`) или нет (`false`).&#x0d;&#x0a;&#x0d;&#x0a;Сейчас параметр используется только с плагином [`pickerui`](masks/plugins/pickerui).&#x0d;&#x0a;&#x0d;&#x0a;Тип данных: `boolean`. Значение по умолчанию: `false`. |
| `effects` | Поле, в котором перечисляются все необходимые [эффекты](masks/effects/overview) маски. Тип данных: `array<obj>`. |
| `plugins` | Поле, в котором перечисляются все необходимые [плагины](masks/plugins/overview) маски. Тип данных: `array<obj>`. |

## Маска на два лица

Чтобы наложить маску на два лица, в [файл конфигурации mask.json](#Файл%20конфигурации%20`mask.json`) добавьте поле `"num_faces": "2"` и в параметре `nface` укажите, какой эффект какому лицу будет принадлежать. Например:

```JSON
{
    "icon": "Icon.png",
    "num_faces": 2,
    "effects": [
        {
            "name": "beautify",
            "nface": 0,
            "mix": 0.6
        },
        {
            "name": "beautify",
            "nface": 1,
            "mix": 0.6
        },
        {
            "name": "patch",
            "anchor": "fullscreen",
            "texture": "Textures/Dust.png"
        },
        {
            "name": "colorfilter",
            "lookup": "ColorFilter/lookup.png",
            "intensity": 0.75
        }
    ]
}
```

По умолчанию параметр `nface` имеет значение `0` (индекс лица начинается с нуля, соответственно второе лицо имеет индекс под номером `1`).

> **Важно!** Распознавание двух лиц для мобильного устройства является ресурсоёмким процессом, который негативно сказывается на плавности изображения камеры.
