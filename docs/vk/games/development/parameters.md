# Параметры запуска

> Источник: [https://dev.vk.ru/ru/games/development/parameters](https://dev.vk.ru/ru/games/development/parameters)
При старте игры или мини-приложения ВКонтакте передаёт им дополнительные параметры, содержащие данные о пользователе и источнике запуска. Значения передаются как URL-параметры на адрес, указанный в настройках игры или мини-приложения.

* [URL запуска игры](games/settings/general/placement#URL)
* [URL запуска мини-приложения](mini-apps/settings/general/placement#URL)

Чтобы получить параметры запуска в коде игры на стороне пользователя, воспользуйтесь одним из способов:

* Извлеките значение свойства `href` объекта `window.location`.
* Вызовите событие [`VKWebAppGetLaunchParams`](bridge/VKWebAppGetLaunchParams).

## Подпись параметров

Чтобы защититься от подмены значений возможной утечки данных, используйте подпись параметров. Подробности — в разделе [Подпись параметров запуска](games/development/parameters-sign).

## Параметры

Некоторые параметры передаются только для игр или только для мини-приложений. Это указано в таблице.

| Параметр | Тип | Описание |
| --- | --- |  --- |
| `sign` | `string` | base64-строка — подпись параметров запуска. Используется для обнаружения подмены параметров третьими лицами. Подробнее — в разделе [Подпись параметров запуска](games/development/parameters-sign).|
| `vk_access_token_settings` | `string` | Список прав доступа пользователя, который запустил приложение. Каждое значение в списке соответствует области данных пользователя, к которой можно получить доступ. Значения в списке разделены запятыми, например `friends,stories,wall`. Список используемых строковых констант приведён в разделе [Права доступа](api/privacy#Права%20доступа). |
| `vk_app_id` | `integer` | Идентификатор запущенного приложения из его настроек. Подробнее: &#x0d;&#x0a; &nbsp;&nbsp; &bullet; [Где найти ID игры](games/settings/overview#Где%20найти%20ID%20игры?) &#x0d;&#x0a; &nbsp;&nbsp; &bullet; [Где найти ID мини-приложения](mini-apps/settings/overview#Где%20найти%20ID%20мини-приложения?) |
| `vk_are_notifications_enabled` | `integer` | Информация о разрешённых уведомлениях от приложения:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — отправка уведомлений запрещена.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — отправка уведомлений разрешена. |
| `vk_chat_id` | `string` | Идентификатор чата. Передаётся, если приложение было запущено из чата. |
| `vk_group_id` | `integer` | Идентификатор сообщества, в котором установлено приложение. Передаётся, если мини-приложение:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Запущено с помощью кнопок в сообществе или из блока приложений сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Запущено по ссылке вида: `vk.com/app6909581_-166562603`. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|
| `vk_has_profile_button` | `integer` |Информация о том, установил ли пользователь кнопку в профиле, по которой запускается мини-приложение. Значение `1` означает, что пользователь установил кнопку в профиле, иначе параметр не передаётся. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|
| `vk_is_app_user` | `integer` |Информация о том, установил ли пользователь приложение :&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — приложение не установлено.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — приложение установлено.&#x0d;&#x0a;&#x0d;&#x0a;После того, как пользователь впервые запустил игру или мини-приложение, они считаются установленными.|
| `vk_is_favorite` | `integer` |Информация о добавлении приложения в избранное:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — приложение не добавлено в избранное.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — приложение добавлено в избранное.|
| `vk_is_play_machine` | `integer` |`1`, если игра была запущена через [Play Machine](games/faq#Поддерживаются%20ли%20Flash-игры?) (специальное приложение для запуска Flash-игр). В ином случае параметр равен `0` или не передаётся. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в мини-приложениях.|
| `vk_is_recommended` | `integer` |Информация о том, [рекомендовал](mini-apps/promotion/user-recommendation) ли пользователь, указанный в параметре `vk_user_id`, мини-приложение своим друзьям:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — мини-приложение не порекомендовано.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — мини-приложение порекомендовано.&#x0d;&#x0a;&#x0d;&#x0a;Если параметр не передан, значит, что приложение ещё не порекомендовано.&#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|
| `vk_is_widescreen` | `integer` |`1`, если игра запущена в [широкоформатном режиме](games/settings/general/display#Широкоформатный%20режим), иначе — `0`. Параметр используется только для [Web-игр](games/getting-started#Direct%20Games%20и%20Web-игры). &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в мини-приложениях.|
| `vk_language` | `string` |Язык пользовательского интерфейса приложения. Возможные значения:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `ru` — русский.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `uk`, `ua` — украинский.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `en` — английский.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `be` — белорусский.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `kz` — казахский.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `pt` — португальский.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `es` — испанский.|
| `vk_platform` | `string` |Платформа, на которой было запущено приложение:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `desktop_web` — десктопная версия сайта ВКонтакте.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_android` — мобильное приложение ВКонтакте для Android.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_ipad` — мобильное приложение ВКонтакте для iPadOS.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_iphone` — мобильное приложение ВКонтакте для iOS.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_web` — мобильная версия сайта ВКонтакте.&#x0d;&#x0a;&#x0d;&#x0a;Если мини-приложение было запущено из VK Мессенджера:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `desktop_app_messenger` — десктопное приложение VK Мессенджер.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `desktop_web_messenger` — десктопная версия VK Мессенджера (web.vk.me).&#x0d;&#x0a; &nbsp;&nbsp; &bullet;`mobile_android_messenger` — мобильное приложение VK Мессенджер для Android.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_iphone_messenger` — мобильное приложение VK Мессенджер для iOS.&#x0d;&#x0a;&#x0d;&#x0a;Если мини-приложение было запущено не из ВКонтакте или VK Мессенджера, то параметр содержит следующие значения:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `android_external` — приложение для Android.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `iphone_external` — приложение для iOS.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `ipad_external` — приложение для iPadOS.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mvk_external` — сайт, открытый в мобильном браузере.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `web_external` — сайт, открытый в десктопном браузере.|
| `vk_profile_id` | `integer` |Идентификатор пользователя, по кнопке в профиле которого запускается мини-приложение. Если мини-приложение запущено не по кнопке в профиле, параметр не передаётся. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|
| `vk_ref` | `string` |Источник запуска приложения. Для игр и мини-приложений есть свой возможный список значений:&#x0d;&#x0a;&nbsp;&nbsp; &bullet; [Список значений для игр](games/development/parameters/vk_ref)&#x0d;&#x0a;&nbsp;&nbsp; &bullet; [Список значений для мини-приложений](mini-apps/development/launch-params/vk_ref)|
| `vk_request_key` | `string` | Используется при старте игры из [запроса о помощи](games/promotion/game-mechanics/requests) или [приглашения](games/promotion/game-mechanics/invites) друга. Указывает ключ запроса или приглашения. Это то же самое значение, которое было передано в параметре `requestKey` при вызове события [`VKWebAppShowRequestBox`](bridge/VKWebAppShowRequestBox) или [`VKWebAppShowInviteBox`](bridge/VKWebAppShowInviteBox) библиотеки [VK Bridge](bridge/overview).&#x0d;&#x0a;&#x0d;&#x0a;Используйте это значение в игре, чтобы предусмотреть сценарий, соответствующий запросу или приглашению, а также для отслеживания конверсии. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в мини-приложениях. |
| `vk_testing_group_id` | `integer` |Идентификатор тестовой группы, в которой состоит пользователь. Если пользователь не состоит в тестовой группе, параметр не передаётся.|
| `vk_ts` | `integer` |Время генерации подписи параметров запуска. Подпись передаётся в параметре `sign`.|
| `vk_user_id`| `integer` | Идентификатор пользователя, запустившего приложение. Параметр `vk_user_id` нужно использовать для авторизации пользователя в мини-приложении или игре. Чтобы проверить подлинность полученных данных, вычислите подпись и сравните с полученным значением sign. |
| `vk_viewer_group_role` | `string` | Роль пользователя в сообществе, из которого запущено мини-приложение:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `admin` — администратор или создатель сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `editor` — редактор сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `member` — подписчик сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `moder` — модератор сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `none` — не состоит в сообществе.&#x0d;&#x0a;&#x0d;&#x0a;Информация о сообществе возвращается, если:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Мини-приложение запущено с помощью кнопок в сообществе.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Исходная ссылка на приложение имела вид: `vk.com/app6909581_-166562603`.&#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх. |

## Передача произвольных параметров

При необходимости вы можете передавать произвольные параметры в игру или мини-приложение.

#### Запуск по прямой ссылке

При запуске игры по прямой ссылке укажите параметры после символа `#` в адресной строке, например:

```
https://vk.com/app1234567#param1=12345
```

Символы после `#` будут переданы игре в URL-параметре `hash`. Этот параметр всегда идёт последним среди остальных передаваемых параметров. Все символы после него являются символами, которые были указаны после `#` в URL игры.  

Получить параметры можно и с помощью свойства `hash` объекта `window.location`. Для отслеживания изменений этого значения в игре используйте события [`VKWebAppLocationChanged`](bridge/VKWebAppLocationChanged) и [`VKWebAppChangeFragment`](bridge/VKWebAppChangeFragment) библиотеки [VK Bridge](bridge/overview).  

Использование знака вопроса (`?`) для передачи параметров при запуске по прямой ссылке не работает.

#### Параметры запуска в настройках игры

Чтобы указать параметры запуска в настройках игры, добавьте в URL знак вопроса и параметры, например:

```
https://my-game.example.com/start-page?param1=12345
```

Получить эти параметры вы можете так же, как и обычные параметры веб-страниц, например с помощью свойства `window.location.search` или с помощью события [`VKWebAppGetLaunchParams`](bridge/VKWebAppGetLaunchParams).

Использовать символы `#` в URL-адресах, которые указываются в настройках игры, нельзя.

## Устаревшие параметры запуска

В разделе с [устаревшей документацией](games/development/parameters/old) приведены параметры, которые больше не используются.

## Материалы по теме

* [Подпись параметров запуска](games/development/parameters-sign)
* [`VKWebAppGetLaunchParams`](bridge/VKWebAppGetLaunchParams)
