# Параметры запуска

> Источник: [https://dev.vk.ru/ru/games/development/parameters/old](https://dev.vk.ru/ru/games/development/parameters/old)
:::note
Параметры, перечисленные в этом разделе, устарели. Перейдите на страницу с обновлённой документацией:

► [Параметры запуска](games/development/parameters)
:::

При старте игры ВКонтакте передаёт ей дополнительные параметры, содержащие данные о пользователе и источнике запуска. Значения передаются как URL-параметры на [указанный в настройках игры адрес](games/getting-started#Укажите%20URL%20для%20загрузки%20игры).

Чтобы получить параметры запуска в коде игры на стороне пользователя, используйте свойство `href` объекта `window.location`.

## Параметры

| Параметр | Тип | Описание |
| --- | --- |  --- |
| `access_token` | `string` | Ключ доступа для отправки API-запросов. Подробнее — в разделе [API — Ключ доступа — Общие сведения](api/access-token/getting-started).|
| `access_token_settings` | `string` | Список прав доступа пользователя, который запустил игру. Каждое значение в списке соответствует области данных пользователя, к которой можно получить доступ. Значения в списке разделены запятыми, например `friends,stories,wall`. Список используемых строковых констант приведён в разделе [Права доступа](api/privacy#Права%20доступа).&#x0d;&#x0a;&#x0d;&#x0a;Значение параметра `access_token_settings` аналогично значению `api_setting`, но использует другой тип данных. |
| `ads_app_id` | `string` | Для служебного использования. |
| `api_id` | `integer` | Идентификатор запущенной игры. Вы можете найти его в [настройках вашей игры](games/getting-started#Скопируйте%20идентификатор%20игры). |
| `api_result` | `string`| Результат первого запроса к API, который выполняется при загрузке игры. Содержимое запроса вы можете указать в настройках игры (раздел **Настройки &rarr; Запрос прав &rarr; Первый запрос к API**). |
| `api_settings` | `integer` | Целое число, которое является битовой маской прав доступа пользователя, запустившего игру. Маска формируется так же, как и [маски прав доступа для API-запросов](api/privacy#Права%20доступа).&#x0d;&#x0a;&#x0d;&#x0a;Значение параметра `api_settings` аналогично значению `access_token_settings`, но использует другой тип данных.|
| `api_url` | `string` |  URL сервиса API ВКонтакте, которому необходимо отправлять API-запросы. |
| `auth_key` | `string` | Значение, которое использовалось ранее для проверки подмены параметров `api_id` и `viewer_id`.&#x0d;&#x0a;  &#x0d;&#x0a;**Важно!** Параметр `auth_key` будет упразднён. Вместо него для проверки подмены используйте параметр [`sign`](#Подпись%20параметров).&#x0d;&#x0a;&#x0d;&#x0a;ВКонтакте пока что продолжает передавать `auth_key` играм при старте для обратной совместимости.|
| `group_id` | `integer` | Идентификатор сообщества, если игра была запущена со страницы сообщества.&#x0d;&#x0a;Если игра была запущена не из сообщества, то параметр равен `0` либо не передаётся. |
| `group_whitelist_scope` | `string` | Для служебного использования. |
| `hash` | `string`| Данные, переданные игре в URL после символа `#`. Такие параметры могут быть переданы при запуске игры [по прямой ссылке](games/access/#Как%20запустить%20игру%20по%20прямой%20ссылке).&#x0d;&#x0a;&#x0d;&#x0a;Параметр `hash` является последним в списке передаваемых URL-параметров. Все символы после него — это символы, указанные после `#` в прямой ссылке. |
| `is_app_user` | `integer` | `1`, если пользователь установил игру, иначе — `0`. |
| `is_favorite` | `integer`| `1`, если пользователь добавил игру в левое меню ВКонтакте и запускает её из левого меню, иначе&nbsp;—&nbsp;`0`. |
| `is_play_machine` | `integer` | `1`, если игра была запущена через [Play Machine](games/faq#Поддерживаются%20ли%20Flash-игры?) (специальное приложение для запуска Flash-игр). В ином случае параметр равен `0` или не передаётся. |
| `is_secure` | `integer` | `1`, если используется защищённое соединение, иначе — `0`. |
| `is_widescreen` | `integer`| `1`, если игра запущена в [широкоформатном режиме](games/settings/general/display#Широкоформатный%20режим), иначе — `0`.&#x0d;&#x0a;&#x0d;&#x0a;Параметр используется только для [Web-игр](games/getting-started#Direct%20Games%20и%20Web-игры).  |
| `language` | `integer`| Идентификатор языка пользователя, просматривающего приложение. Возможные значения — те же, что используются в [API-запросах](api/api-requests#Общие%20параметры). |
| `lc_name` | `string` | Для служебного использования. |
| `odr_enabled` | `integer` | Используется на iOS. `1` означает, что игра поддерживает [технологию On-Demand Resources (ODR)](games/direct-games/on-demand-resources). Если игра не поддерживает эту технологию, то параметр не передаётся. |
| `parent_language` | `integer`| Идентификатор языка, которым можно заменить язык пользователя (возвращается для неофициальных языковых версий). Возможные значения — те же, что используются в [API-запросах](api/api-requests#Общие%20параметры). |
| `platform` | `string`| Идентификатор платформы, на которой запускается игра. Возможно одно из следующих значений:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `html5_ios` — мобильное приложение ВКонтакте для iOS.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `html5_android` — мобильное приложение ВКонтакте для Android.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `html5_mobile` — мобильная версия сайта ВКонтакте.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `web` — десктопная версия сайта ВКонтакте. |
| `ref_notification_id` | `integer`| Используется при старте игры из сообщения рассылки. Указывает идентификатор рассылки.&#x0d;&#x0a;&#x0d;&#x0a;Если рассылка была выполнена с помощью API-запроса [`secure.sendNotification`](method/secure.sendNotification), значение параметра совпадает с параметром `notification_id` этого запроса.&#x0d;&#x0a;&#x0d;&#x0a;Если рассылка была создана в настройках игры, параметр содержит идентификатор подписки, сгенерированный ВКонтакте.&#x0d;&#x0a;&#x0d;&#x0a;Идентификаторы рассылок разных типов могут совпадать, поэтому при анализе значений проверяйте и параметр `ref_notification_type`. |
| `ref_notification_type` | `string` | Используется при старте игры из сообщения рассылки. Указывает тип рассылки. Возможно одно из следующих значений:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `auto` — массовая рассылка, созданная в настройках игры.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `custom` — рассылка выполнена с помощью API-запроса [`secure.sendNotification`](method/secure.sendNotification).&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `trigger` — автоматическое уведомление, созданное в настройках игры. |
| `referrer` | `string`| Обозначение источника перехода в игру.  Возможные значения — те же, что используются в [API-запросах](api/app-run-parameters#referrer). |
| `request_key` | `string` | Используется при старте игры из [запроса о помощи](games/promotion/game-mechanics/requests) или [приглашения](games/promotion/game-mechanics/invites) друга. Указывает ключ запроса или приглашения. Это то же самое значение, которое было передано в параметре `requestKey` при вызове события [`VKWebAppShowRequestBox`](bridge/VKWebAppShowRequestBox) или [`VKWebAppShowInviteBox`](bridge/VKWebAppShowInviteBox) библиотеки [VK Bridge](bridge/overview).&#x0d;&#x0a;&#x0d;&#x0a;Используйте это значение в игре, чтобы предусмотреть сценарий, соответствующий запросу или приглашению, а также для отслеживания конверсии.  |
| `secret` | `string` | Для служебного использования. |
| `sid` | `string`| Для служебного использования.  |
| `sign` | `string`| base64-строка — подпись параметров запуска. Используется для обнаружения подмены параметров третьими лицами. Подробнее — в разделе [Подпись параметров](#Подпись%20параметров). |
| `sign_keys` | `string`| Список имён параметров, которые необходимы для [вычисления подписи](#Подпись%20параметров) в параметре `sign`.&#x0d;&#x0a;&#x0d;&#x0a;Имена параметров в списке разделены запятой. |
| `stats_hash` | `string` | Для служебного использования. |
| `timestamp` | `integer`| Время создания [подписи](#Подпись%20параметров) в параметре `sign`. Это время представлено в Unix-формате — целое число, обозначающее количество миллисекунд, прошедших с 00:00:00 1 января 1970 года. |
| `user_id` | `integer`| Идентификатор  пользователя, со страницы которого игра была запущена. Если игра стартовала не со страницы пользователя, то параметр равен `0`. |
| `viewer_id` | `integer`| Идентификатор пользователя, который запустил игру. |
| `viewer_type` | `integer` | Тип пользователя, который запустил игру.&#x0d;&#x0a;&#x0d;&#x0a;Если пользователь `viewer_id` запускает игру со страницы сообщества, то есть если `group_id` не равен `0`, то значение `viewer_type` может быть следующим:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — пользователь не состоит в сообществе.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — пользователь —  участник сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `2` — пользователь — модератор сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `3` — пользователь — редактор сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `4` — пользователь — создатель или администратор сообщества.&#x0d;&#x0a;&#x0d;&#x0a;Если пользователь запускает игру не со страницы сообщества, то есть если `user_id` не равен `0`, а `group_id` равен `0`, то `viewer_type` может иметь одно из следующих значений:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — пользователь `viewer_id` находится в друзьях у пользователя `user_id`.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `2` — пользователь `viewer_id` — администратор игры либо запустил игру со своей страницы.&#x0d;&#x0a; &bullet; &nbsp;&nbsp; `0` или параметр не указан — остальные случаи. `0` используется, например, когда игру запускает любой пользователь по [прямой ссылке](games/access/#Как%20запустить%20игру%20по%20прямой%20ссылке).|
| `whitelist_scopes` | `string` | Для служебного использования. |

## Подпись параметров

### Что такое подпись

Игры ВКонтакте используют подпись параметров, чтобы защититься от подмены значений и возможной утечки данных.

1. При запуске игры ВКонтакте вычисляет подпись с помощью [защищённого ключа доступа](games/settings/development/keys#Защищённый%20ключ) из настроек игры. Этот ключ известен авторам игры, но неизвестен третьим лицам.

    Полученное значение передаётся игре в параметре `sign`.

2. Игра при старте может вычислить подпись по такому же алгоритму и сравнить это значение с переданным `sign`.

    Разница будет означать подмену.

Ранее для такой проверки использовался параметр `auth_key`,  но он защищал только от подмены значений `api_id` и `viewer_id`.

### Как вычислить подпись

Для вычисления подписи используются параметры, имена которых переданы в `sign_keys`. Чтобы получить подпись, выполните следующие шаги:

1. Извлеките параметры, перечисленные в `sign_keys`, и объедините пары `параметр=значение` в одну строку в том же порядке, в котором они следуют в `sign_keys`. Используйте амперсанд (`&`) в качестве разделителя.

    > Обратите внимание, что строковые значения должны быть в URL-кодировке. Например, запятые должны быть представлены как `%2C`, пробелы — как `%20`.  

1. Для получившейся строки вычислите хеш-код, используя алгоритм SHA256 с методом HMAС. При расчётах используйте защищённый ключ из настроек вашей игры (раздел **Настройки**, поле **Защищённый ключ**).

1. Результирующее значение закодируйте в base64-строку.

Получившаяся строка должна совпадать с переданной в параметре `sign`.

## Примеры проверки подписи

### PHP

```PHP
<?php

$client_secret = 'xxxxx';

$url = (isset($_SERVER['HTTPS']) && $_SERVER['HTTPS'] === 'on' ? "https" : "http") . "://$_SERVER[HTTP_HOST]$_SERVER[REQUEST_URI]";

// Получаем параметры из URL
$query_params = [];
$parsed = parse_url($url, PHP_URL_QUERY);
parse_str($parsed, $query_params); 

$sign_params = [];
$sign_keys = explode(',', $query_params['sign_keys']);
foreach ($sign_keys as $key) {
  $sign_params[$key] = $query_params[$key];
}

// Формируем строку вида "param1=value1&param2=value2"
$sign_params_query = http_build_query($sign_params); 

// Вычисляем подпись, используя защищённый ключ приложения
$sign = rtrim(strtr(base64_encode(hash_hmac('sha256', $sign_params_query, $client_secret, true)), '+/', '-_'), '='); 

// Сравниваем полученную подпись со значением параметра sign
$sign_status = $sign === $query_params['sign']; 

echo 'status: ' . ($sign_status ? 'ok' : 'fail')."\n";

?>
```

### JavaScript (Node.js)

```JavaScript
const qs = require('querystring');
const crypto = require('crypto');

// Получаем параметры из URL
const urlParams = qs.parse(URL_PARAMS);
const signKeys = new URLSearchParams(URL_PARAMS)
    .get('sign_keys')
    .split(',');

const ordered = {};

signKeys.forEach(key => ordered[key] = urlParams[key]);

// Формируем строку вида "param1=value1&param2=value2"
const stringParams = qs.stringify(ordered);

// Вычисляем подпись, используя защищённый ключ приложения
const paramsHash = crypto
    .createHmac('sha256', secretKey)
    .update(stringParams)
    .digest()
    .toString('base64')
    .replace(/\+/g, '-')
    .replace(/\//g, '_')
    .replace(/=$/, '');

// Сравниваем полученную подпись со значением параметра sign
if (paramsHash == urlParams.sign) {
    console.log("Подпись верна.");
} else {
    console.log("Подпись невалидна, возможна подмена параметров.");
}

```

<!-- 
## Параметр `auth_key`

Параметр `auth_key` использовался ранее для проверки подмены значений `api_id` и `viewer_id`. Он вычисляется на сервере ВКонтакте следующим образом:

```JavaScript
auth_key = md5(api_id + '_' + viewer_id + '_' + api_secret)
```

В этой формуле:

* `api_id` — идентификатор вашей игры.

* `viewer_id` — идентификатор пользователя, запустившего игру.

* `api_secret` — защищённый ключ игры. Вы можете узнать его в настройках игры ВКонтакте (раздел **Настройки**, поле **Защищённый ключ**).

> Параметр `auth_key` будет упразднён. Вместо него для проверки подмены используйте параметр [`sign`](#Подпись%20параметров).&#x0d;&#x0a;&#x0d;&#x0a;ВКонтакте пока что продолжает передавать `auth_key` играм при старте для обратной совместимости.
-->

## Передача произвольных параметров

При необходимости вы можете передавать произвольные параметры в игру.

#### Запуск по прямой ссылке

При запуске игры по прямой ссылке укажите параметры после символа `#` в адресной строке, например:

```
https://vk.com/app1234567#param1=12345
```

Символы после `#` будут переданы игре в URL-параметре `hash`. Этот параметр всегда идёт последним среди остальных передаваемых параметров. Все символы после него являются символами, которые были указаны после `#` в URL игры.  

Получить параметры можно и с помощью свойства `hash` объекта `window.location`. Для отслеживания изменений этого значения в игре используйте события [`VKWebAppLocationChanged`](bridge/VKWebAppLocationChanged) и [`VKWebAppChangeFragment`](bridge/VKWebAppChangeFragment) библиотеки [VK Bridge](bridge/overview).  

Использование знака вопроса (`?`) для передачи параметров при запуске по прямой ссылке не работает.

#### Параметры запуска в настройках игры

Чтобы указать параметры запуска в настройках игры, добавьте в URL знак вопроса и параметры, например:

```
https://my-game.example.com/start-page?param1=12345
```

Получить эти параметры вы можете так же, как и обычные параметры веб-страниц, например с помощью свойства `window.location.search`.

Использовать символы `#` в URL-адресах, которые указываются в настройках игры, нельзя.
