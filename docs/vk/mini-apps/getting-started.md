# Первые шаги

> Источник: [https://dev.vk.ru/ru/mini-apps/getting-started](https://dev.vk.ru/ru/mini-apps/getting-started)
>В связи с ростом количества пользователей, сообществ и сервисов ВКонтакте мы рекомендуем хранить идентификаторы пользователей и других необходимых значений в диапазоне `Int64` вместо `Int32`. Подробнее — в статье [Переход на LongID](general/long-id).

В этом разделе вы узнаете, как добавить ваше первое мини-приложение на площадки ВКонтакте и Одноклассники. Эта инструкция включает шаги создания приложения из шаблона, регистрацию и размещение на хостинге.

Подробнее о полном процессе создания приложения от идеи до монетизации — в нашем видеокурсе:

&nbsp;&nbsp;&nbsp;&nbsp;► [Разработка мини-приложений ВКонтакте](mini-apps/learning/course)

## О мини-приложениях

Мини-приложения ВКонтакте и в Одноклассниках — это веб-приложения, которые запускаются внутри интерфейса социальных сетей. Клиентская часть представляет собой обычные веб-страницы, а серверная может быть реализована на любой современной технологии разработки.

При регистрации мини-приложению автоматически присваивается идентификатор. Оно открывается по специальному адресу и загружается внутри ВКонтакте или Одноклассников.

Для взаимодействия с площадками мини-приложения используют их API — например, для запроса разрешений или доступа к данным пользователя. Чтобы упростить обмен данными, используйте библиотеку [VK Bridge](bridge/overview) — она работает на стороне клиента и позволяет отправлять запросы и получать ответы.

## Что потребуется

* Навыки работы с командной строкой. Используйте окно командной строки операционной системы либо редактор кода, в котором есть командная строка, например [Visual Studio Code](https://code.visualstudio.com/).

* Node.js. Скачать и установить эту среду можно с [официального сайта nodejs.org](https://nodejs.org).   

    > Мы рекомендуем использовать версию Node.js 18.0.0 или более позднюю.

* Мобильное устройство для подтверждения действий при регистрации мини-приложения и работе с его настройками.

## 1. Создайте проект мини-приложения

Начните с создания мини-приложения на своём компьютере. Вы можете применять любые среды разработки, языки и технологии, которые используются для создания HTML5-приложений.

Рассмотрим, как создать проект мини-приложения из шаблона с помощью пакета [create-vk-mini-app](mini-apps/getting-started/create-vk-mini-app). Результирующий проект представляет собой npm-пакет, в котором подключены все необходимые библиотеки. Функциональность этого мини-приложения включает два экрана с простой навигацией и кнопкой, которая показывает стикер.

1. Откройте окно командной строки и перейдите в папку, в которой хотите создать проект мини-приложения:

    ```Командная&nbsp;строка
    cd <путь-к-папке>
    ```

1. Запустите скрипт создания приложения из шаблона.

    ```Командная&nbsp;строка
    npx @vkontakte/create-vk-mini-app
    ```

    При первом использовании появится запрос на скачивание пакета @vkontakte/create-vk-mini-app. Нажмите **Enter**, чтобы подтвердить.

1. Далее скрипт последовательно запросит следующие данные:

    * Название папки, в которой будут размещены файлы приложения. Для простоты введите точку — тогда проект будет создан в текущей папке. Вы также можете использовать предлагаемое имя — `mini-app`. В этом случае скрипт создаст для проекта подпапку `mini-app` в текущей папке.

        По умолчанию имя папки будет использовано и для именования результирующего пакета. Если же имя не может быть использовано для именования пакетов, скрипт попросит вас ввести название проекта. Это название будет указано в файле `package.json` из корневой папки проекта после создания.

    * Язык программирования: TypeScript или JavaScript. Выберите тот язык, который вам знаком. Это не принципиально для создаваемого проекта.

    * Структурный шаблон — набор библиотек, которые будут подключены к приложению. Выберите рекомендуемый вариант — **VKUI+Bridge+Router**.

    * Скрипт создаст файлы приложения в указанной вами папке и покажет дальнейшие инструкции. Выполним только первые две из них.

        * Если вы указали имя проекта при его создании, перейдите в созданную папку проекта:

            ```Командная&nbsp;строка
            cd <папка-проекта>
            ```

        * Установите все пакеты, указанные в файле `package.json` вашего проекта:

            ```Командная&nbsp;строка
            npm install
            ```

            Подождите, пока пакеты установятся. Выполнять `npm run start` пока не нужно.

[При создании проекта получаю ошибку на Windows 10 Pro. Что делать?](#После%20выполнения%20команды%20создания%20проекта%20мини-приложения%20на%20Windows%2010%20Pro%20получаю%20ошибку.%20Что%20делать?)

Проект вашего мини-приложения готов.

Обратите внимание, что при вызове create-vk-mini-app вы можете добавить дополнительные параметры командной строки, чтобы указать желаемые характеристики приложения. Подробнее — в [описании пакета create-vk-mini-app](mini-apps/getting-started/create-vk-mini-app).

:::note
**Важно!** Для корректной работы мини-приложения к нему необходимо подключить библиотеку [VK Bridge](bridge/getting-started), а также вызвать событие инициализации [`VKWebAppInit`](bridge/VKWebAppInit) в начале работы мини-приложения. Если не вызвать событие, мини-приложение не сможет взаимодействовать с площадками ВКонтакте и Одноклассники.

В приложении, созданном с помощью create-vk-mini-app, уже есть библиотека VK Bridge и вызов события инициализации.
:::

Если вы будете создавать мини-приложение без использования пакета create-vk-mini-app, подключите VK Bridge и самостоятельно в коде приложения вызовите событие инициализации. Подробнее — в [описании библиотеки VK Bridge](bridge/getting-started).

## 2. Зарегистрируйте мини-приложение

1. Откройте [десктопную версию сайта ВКонтакте](https://vk.com) и войдите в свой аккаунт.
1. Откройте [страницу со списком приложений](https://dev.vk.com/ru/admin/apps-list) либо перейдите на портал разработчиков [dev.vk.com](https://dev.vk.com) и выберите **Приложения** в верхнем меню.
1. В левом меню нажмите **Создать приложение**.

    <!-- /exclusions/_images/mini-apps/getting-started/create-app.png -->
    ![alt=Создание мини-приложения;title=Создание мини-приложения](d4a7d1923bab31fde37ba77a0d42ec1a3d004d6a98b73029a28c63a6 "-2284359600979040897")

1. Заполните свойства мини-приложения:

    * В поле **Тип** выберите **Мини-приложение**.
    * Укажите название приложения. Оно может содержать только буквы, цифры, дефисы, пробелы, двоеточия и тире. Вы можете указать любое имя и изменить его позже в разделе [Настройки &rarr; Информация](mini-apps/settings/general/information).
    * Выберите категорию мини-приложения.
    * Нажмите **Создать**.

        Во всплывающем окне **Подтверждение действия** выберите способ подтверждения и одобрите действие.

        > Если вы выбрали подтверждение с помощью мобильного приложения ВКонтакте, push-уведомление придёт, только если приложение запущено на вашем смартфоне.  

Регистрация приложения завершена. Откроется панель управления приложением. Идентификатор мини-приложения генерируется автоматически при регистрации. Он необходим для запуска и других операций с приложением.

<!-- exclusions/_images/mini-apps/getting-stared/configure-app.png -->
![alt=Панель управления приложением;title=Панель управления приложением](04e5cb736e8920e36b5d57fd243f743417e00b2dd5ebaa3be4912edc "-8144275086638920030")

Чтобы разместить мини-приложение в Одноклассниках, перейдите в раздел [Настройки &rarr; Другие площадки](mini-apps/settings/general/other-sites) и нажмите **Привязать OK ID**. Подробнее о подключении мини-приложения к Одноклассникам — в разделе [Публикация в Одноклассниках](ok/publishing#Шаг%202.%20Подключите%20приложение%20к%20Одноклассникам).

## 3. Запустите мини-приложение через туннель

Ваше мини-приложение пока находится на вашем компьютере. Чтобы сделать его доступным извне, необходимо настроить специальное соединение — туннель. Для создания такого соединения мы разработали библиотеку — [VK Tunnel](libraries/tunnel).

:::note
**Из-за технических работ с 2 октября 2025 года VK Tunnel недоступен**

При подключении сервис вернёт ошибку. Для отладки и демонстрации ваших веб-приложений рекомендуем временно использовать сторонние решения.

Вы можете пропустить этот шаг и сразу разместить мини-приложение на хостинге.
:::

#### 3.1. Подготовьте проект

1. Если вы работаете в Windows, установите дополнительные инструменты разработчика.

    Запустите **Пуск &rarr; Все приложения &rarr; Node.js &rarr; Install Additional Tools for Node.js** и следуйте инструкциям на экране.

    Альтернативный способ:

    * Загрузите установщик Visual Studio Build Tools: https://aka.ms/vs/17/release/vs_BuildTools.exe.

    * Запустите его и выберите для установки компонентов **Средства сборки Node.js** и **Разработка классических приложений на C++**.

1. Установите VK Tunnel. Для этого перейдите в папку проекта мини-приложения и выполните следующую команду:

    ```Командная&nbsp;строка
    npm install @vkontakte/vk-tunnel --include=dev
    ```

1. Добавьте в файл `package.json` вашего проекта скрипт `tunnel` и сохраните изменения:

    ```json
    "scripts": {
      ...
      "tunnel": "vk-tunnel --insecure=1 --http-protocol=http --ws-protocol=ws --host=0.0.0.0 --port=5173"
    }
    ```
    
    В приложении, созданном из шаблона с помощью create-vk-mini-app, скрипт `tunnel` уже добавлен. Если параметры скрипта отличаются от указанных, обновите их.

#### 3.2. Запустите мини-приложение локально

Локальный запуск необходим для дальнейшего запуска внутри ВКонтакте.

1. В окне командной строки перейдите в папку проекта мини-приложения:

    ```Командная&nbsp;строка
    cd <путь-к-папке-проекта>
    ```

1. Выполните следующую команду:

    ```Командная&nbsp;строка
    npm run start -- --host
    ```

    Она запустит сервер мини-приложения на вашем компьютере.

    > **Важно!** Не пропустите параметр `-- --host`. Он необходим для работы.

    Если всё в порядке, вы увидите следующее в окне командной строки:

    ```Окно&nbsp;командной&nbsp;строки
    VITE v5.2.10  ready in 441 ms
    ➜  Local:   http://localhost:5173/
    ➜  Network: http://100.75.14.91:5173/
    ➜  press h + enter to show help
    ```

Не закрывайте окно командной строки, иначе сервер мини-приложения остановится. Он должен работать для последующего запуска.

> [Если открыть указанные адреса, приложение не загрузится полностью. Почему?](#Приложение%20не%20грузится%20до%20конца%20при%20локальном%20запуске.%20Почему?)

#### 3.3. Запустите VK Tunnel

1. **Важно!** Откройте ещё одно окно командной строки. Не закрывайте окно, в котором вы запустили сервер мини-приложения локально и не останавливайте работу сервера.

    В новом окне перейдите в папку проекта мини приложения и затем запустите VK Tunnel:

    ```Командная&nbsp;строка
    cd <путь-к-папке-проекта>
    ```

    ```Командная&nbsp;строка
    npm run tunnel
    ```

    > Возможные ошибки и решения:
    > * [Получаю ошибку `sh: 1: vk-tunnel: Permission denied`. Что делать?](#Получаю%20ошибку%20sh:%201:%20vk-tunnel:%20Permission%20denied.%20Что%20делать?)    
    > * [Получаю ошибку `Error: Пользователь не является создателем приложения`. Что делать?](#Получаю%20ошибку%20Error:%20Пользователь%20не%20является%20создателем%20приложения.%20Что%20делать?)    
    > * [Получаю ошибку `Error: connect ECONNREFUSED 127.0.0.1:10888`. Что делать?](#Получаю%20ошибку%20Error:%20connect%20ECONNREFUSED%20127.0.0.1:10888.%20Что%20делать?)

1. В командной строке вы увидите ссылку для авторизации:

    <!-- exclusions/_images/mini-apps/getting-stared/tunnel-auth-link.webp -->
    ![alt=Пример ссылки для авторизации;title=Пример ссылки для авторизации](da625c91528aeb52db461d07e2977577e45f5dafb73aee8706fdd5f9 "2441964091301451021")

    У вас она будет выглядеть немного иначе: значение `code` меняется от запуска к запуску и от компьютера к компьютеру.

    Откройте ссылку в браузере. Вы увидите сообщение об успешной авторизации.

1. Вернитесь в окно командной строки и подтвердите действие: введите **Y** или **y**, или нажмите **Enter**.

    Если всё в порядке и VK Tunnel запущен, вы получите сообщение со ссылкой на мини-приложение:

    ```Окно&nbsp;командной&nbsp;строки
    open: https://user743784479-cx6qjdfo.wormhole.vk-apps.com/
    ```

    Это значит, что VK Tunnel отобразил в интернете веб-сервер, работающий на вашем локальном компьютере. После этого можно запустить мини-приложение внутри ВКонтакте.

#### 3.4. Обновите настройки мини-приложения

1. В браузере перейдите в [настройки мини-приложения](mini-apps/settings/overview).

1. Откройте раздел [Настройки &rarr; Размещение](mini-apps/settings/general/placement). В блоке **Десктопная версии сайта** включите **Режим разработки** и укажите полученную на предыдущем шаге ссылку как URL для разработки. То же самое можно сделать и для других платформ.

<!-- exclusions/_images/mini-apps/getting-stared/dev-url.png -->
![alt=Настройка «Режим разработки»;title=Настройка «Режим разработки»](c9a52ebd12c5a876b56f16c1185025f87aff7090284c58502a70d57f "8947336502836321134")

URL, которые вы указываете в разделе [Размещение](mini-apps/settings/general/placement), используются для запуска мини-приложения. Именно этот адрес будет передан в элемент `<iframe>` или компонент WebView. Когда включён режим разработки, для администраторов приложения будет использован адрес, указанный для этого режима. Для остальных пользователей приложение будет загружаться по адресу, указанному в поле **URL**.

#### 3.5. Запустите мини-приложение ВКонтакте

Запустить мини-приложение можно прямо из панели управления. 

<!-- exclusions/_images/mini-apps/getting-stared/launch-app.png -->
![alt=Запуск мини-приложения;title=Запуск мини-приложения](4db1d6aae1df7dfe9c334f0e88157d4bd03b8793ebcfd68dcd780380 "2237948144068439621")

#### Замечания

* Мини-приложение будет работать, только пока оно и VK Tunnel запущены на вашем локальном компьютере. Чтобы мини-приложение работало автономно, вам потребуется разместить его на хостинге.

* Если в браузере отображается ошибка **Bad gateway**, проверьте, что мини-приложение запущено, а если ошибка **Error 1**, — проверьте VK Tunnel.

* Ссылку, которую вы получили — `https://vk.com/app{ID}` — можно использовать, чтобы [поделиться мини-приложением с друзьями](#Как%20поделиться%20мини-приложением%20с%20друзьями?).

## 4. Разместите мини-приложение на хостинге 

Чтобы мини-приложение работало автономно и не зависело от работы туннеля, приложение необходимо разместить на постоянно доступном сервере. Возможные пути решения:

* Воспользоваться услугами хостинговых компаний. Вам нужно выбрать тариф с поддержкой HTTPS.

* Разместить мини-приложение в [облаке VK Cloud](https://cloud.vk.com/docs/ru/additionals/start).

* Использовать хостинг, который мы предлагаем для мини-приложений. Файлы будут размещаться в CDN-инфраструктуре компании. Решение бесплатно, но есть ограничения на размер выкладываемых файлов и частоту обновлений. Давайте рассмотрим, как им можно воспользоваться.

    :::note
    Обращаем ваше внимание, что мы предоставляем место для хранения файлов, но не вычислительные мощности для работы сервера. Если вашему приложению нужно выполнять обработку данных на сервере, воспользуйтесь другими возможностями для хостинга.
    :::

Для работы с хостингом мы создали библиотеку [vk-miniapps-deploy](https://github.com/VKCOM/vk-miniapps-deploy). Она включена в ваше мини-приложение, потому что оно было создано с помощью create-vk-mini-app.

Если по какой-то причине библиотеки нет в вашем проекте, вы можете установить её самостоятельно. Откройте окно командной строки, перейдите в папку проекта и установите vk-miniapps-deploy:

```Командная&nbsp;строка
cd <путь-к-папке-проекта>

npm install @vkontakte/vk-miniapps-deploy --include=dev
```

#### 4.1. Настройте проект для работы с хостингом 

1. В каталоге проекта откройте файл `vk-hosting-config.json`.

1. В поле `app_id` замените `0` на идентификатор вашего мини-приложения. Сохраните изменения.

    ```Файл&nbsp;vk-hosting-config.json
    {
      "static_path": "build",
      "app_id": 535577**, 
      "endpoints": {
        "mobile": "index.html",
        "mvk": "index.html",
        "web": "index.html"
      }
    }
    ```

    Подробнее о значениях в файле — в разделе [Файл vk-hosting-config.json](mini-apps/development/hosting/config-file).

#### 4.2. Выложите файлы

1. Выполните следующую команду. Она запустит скрипт для размещения файлов на хостинге:

    ```Командная&nbsp;строка
    npm run deploy
    ```

    Сначала скрипт вызовет `npm run build` для сборки проекта. В результате в папке проекта появится подпапка `build`, а также архив `build.zip`. Он содержит файлы, которые будут выложены на хостинг.

    <!-- exclusions/_images/mini-apps/getting-stared/message-about-building.webp -->
    ![alt=Сообщения о сборке;title=Сообщения о сборке](4c5b570afe10e1ef498de29a885b14155168c78acd34c6adf29118c6 "-1992446003524542770")

1. Разрешите размещение файлов на хостинге.

    <!-- exclusions/_images/mini-apps/getting-stared/upload-to-hosting-request.webp -->
    ![alt=Запрос на размещение файлов на хостинге;title=Запрос на размещение файлов на хостинге](ab74d5c5f2ab1a4548dbd8dc849639e5b9c2a15d9c20dbc3ca7ec752 "5377248240225883950")

1. Далее скрипт последовательно запросит разрешения на автоматическое изменение URL в настройках размещения мини-приложения. Примите предложения:

    * `Would you like to update prod urls?` — обновить адрес мини-приложения.
    * `Would you like to update dev urls?` – обновить адрес мини-приложения в режиме разработки.
    * `Would you like to update test group url?` – обновить адрес мини-приложения для тестовой группы. Если у вас еще нет тестовых групп, изменений не произойдет.
    * `Enter test group name` – если на предыдущем шаге вы выбрали обновить адрес для тестовой группы, укажите ее название. Вы также можете не указывать название, в этом случае адрес тестовой группы не поменяется.

    <!-- exclusions/_images/mini-apps/getting-stared/update-url-requests.webp -->
    ![alt=Запросы на изменение URL;title=Запросы на изменение URL](e867e78a35d84232fb29c1b01cb80aad14270d9bd272c4edcaacd508 "-3828280852469093901")

    > При каждом запуске команды для размещения файлов на хостинге, например после внесения изменений, необходимо подтверждать изменение URL. Это связано с тем, что при обновлении файлов хеш в URL приложения изменяется.

1. При первом выполнении пакет vk-miniapps-deploy запустит процедуру получения ключа доступа. Само получение проходит автоматически. Вам необходимо только открыть URL в браузере.

    <!-- exclusions/_images/mini-apps/getting-stared/access-token-request.webp -->
    ![alt=URL для получения ключа доступа;title=URL для получения ключа доступа](9f6a4c5e96bd6c32dba5aca7672ffaa66b06e622d6be8d1a9a534bbb "-7830015235439943493")

    Скопируйте URL и откройте в браузере. Вы увидите сообщение о том, что VK Mini Apps Hosting получило доступ к вашему аккаунту.

    Вернитесь в окно командной строки и нажмите **y** или **Enter**, чтобы подтвердить, что авторизация выполнилась успешно. Вы увидите сообщение, что ключ получен.

1. Далее скрипт загрузит файлы на хостинг и отобразит URL, которые соответствуют файлам на хостинге.

    Для изменения URL, которые используются в режиме разработки, ваше подтверждение не нужно, а вот для изменения production URL — нужно. Скрипт отправит запрос на ваше мобильное устройство. Подтвердите изменения в мобильном приложении ВКонтакте.

    <!-- exclusions/_images/mini-apps/getting-stared/resulting-urls.webp -->
    ![alt=Результирующие URL в окне командной строки;title=Результирующие URL в окне командной строки](ca52235b5978476f403927c58d7b11eee9f4318ecc907a8c58bec79c "210083739503949222")

    Так как мы разрешили автоматическое обновление URL приложения в настройках мини-приложения, то скрипт добавил эти же URL и в настройки. Убедитесь в этом сами: откройте страницу [Настройки &rarr; Размещение](mini-apps/settings/general/placement) и посмотрите URL для различных платформ.

#### 4.3 Запустите мини-приложение

Сразу после регистрации мини-приложение доступно ВКонтакте только его создателю и администраторам. Если вы подключили мини-приложение к Одноклассникам, то там оно доступно только создателю. Запустить мини-приложение можно по прямой ссылке, но не ранее чем через 15 минут после создания ссылки на сайте Одноклассники. 

Способ запуска зависит от места запуска: десктопная или мобильная версии сайтов ВКонтакте и Одноклассники или мобильное приложение для Android или iOS.

Запустить мини-приложение можно из панели управления. Проще всего это сделать в десктопной версии сайта: перейдите на страницу [Приложения](https://dev.vk.com/ru/admin/apps-list) и нажмите **Настройки** напротив нужного приложения. Затем скопируйте адрес мини-приложения для нужной площадки.

<!-- exclusions/_images/mini-apps/getting-started/launch.png -->
![alt=Запуск мини-приложения из панели управления;title=Запуск мини-приложения из панели управления](c94a5e530ae1b5789dc143b4ded374aeee542dfea17dd5235e7cb3b9 "5655436147430724603")

Мини-приложение откроется в интерфейсе ВКонтакте или Одноклассников.

<!-- exclusions/_images/mini-apps/getting-started/app-in-vk.png -->
![alt=Экран мини-приложения ВКонтакте;title=Экран мини-приложения ВКонтакте](a100d62b07bc400795c4015055b890b85212d369851e52720ca189ca "1270097662057719440")

Чтобы запустить неопубликованное мини-приложение из UI мобильного приложения ВКонтакте, отправьте ссылку на мини-приложение в чат **Избранное** и запустите его по этой ссылке. Таким же способом можно запустить мини-приложение в Одноклассниках.

> [Как поделиться мини-приложением с друзьями?](#Как%20поделиться%20мини-приложением%20с%20друзьями?)

## Краткая инструкция

1. Создайте проект мини-приложения из шаблона:

    ```Командная&nbsp;строка
    npx @vkontakte/create-vk-mini-app
    ```

1. [Зарегистрируйте](https://dev.vk.com/ru/admin/apps-list) мини-приложение и получите его [идентификатор](mini-apps/settings/overview#Где%20найти%20ID%20мини-приложения?).

1. Загрузите файлы приложения на хостинг с помощью vk-miniapps-deploy:

    * В файле `vk-hosting-config.json` в поле `app_id` замените `0` на идентификатор мини-приложения.

    * Запустите скрипт загрузки:

        ```Командная&nbsp;строка
        npm run deploy
        ```

1. Откройте ссылку `https://vk.com/app{ID}`, чтобы проверить, что мини-приложение работает.

## Что дальше

* Мы создали видеокурс, который охватывает все этапы создания мини-приложения: от выбора идеи до реализации, продвижения и вывода средств. Рекомендуем посмотреть.

    &nbsp;&nbsp;&nbsp;&nbsp;► [Видеокурс «Разработка мини-приложений ВКонтакте»](mini-apps/learning/course)

* Если вы ещё не работали с React, ознакомьтесь с [официальным руководством по React](https://ru.react.dev/learn).

* Если вы уже начали разработку, ознакомьтесь со [списком рекомендаций](mini-apps/catalog/moderation) для размещения мини-приложений в каталогах.

## FAQ

#### Не удалось выполнить инструкцию, получаю ошибки. Что делать?

Убедитесь, что вы используете версию [Node.js 14.18.0](mini-apps/software-installation#Как%20сменить%20версию%20Node.js?) или более позднюю. Версия 14.18.0 — минимально необходимая версия для работы библиотеки [VK Bridge](bridge/overview). Однако [пакет create-vk-mini-app](mini-apps/getting-started/create-vk-mini-app), который используется для создания проекта мини-приложения из шаблонов, требует версию Node.js 18.0.0 или более позднюю.

Если всё равно возникают ошибки, поищите ответ в сообществе [VK Mini Apps](https://vk.com/vkappsdev).

#### После выполнения команды создания проекта мини-приложения на Windows 10 Pro получаю ошибку. Что делать?

Если вы используете операционную систему Windows 10 Pro и после выполнения команды `npx @vkontakte/create-vk-mini-app` вы получили ошибку:

1. Перейдите в созданную папку проекта мини-приложения:

    ```Командная&nbsp;строка
    cd <путь-к-папке>
    ```

1. Установите зависимости Node.js:

    ```Командная&nbsp;строка
    npm install
    ```

1. Установите модули из файла `package-lock.json`:

    ```Командная&nbsp;строка
    npm ci
    ```

После того как вы выполните эти команды, вы сможете продолжить выполнять инструкцию.

#### Приложение не грузится до конца при локальном запуске. Почему?

Скорее всего вы попытались открыть мини-приложение не из ВКонтакте UI по адресу, который сборщик Vite показывает в строке Local или Network.

В демонстрационных целях мини-приложение, созданное из шаблона, при старте вызывает события VK Bridge для получения информации о пользователе, который запустил мини-приложение. При запуске вне ВКонтакте этот код работать не будет. Поэтому вы видите страницу, которая не загружается до конца.

Для локальной отладки вы можете использовать библиотеку [`vk-bridge-mock`](mini-apps/development/debugging#Отладка%20при%20помощи%20vk-bridge-mock), которая позволит эмулировать получение тестовых данных. 

#### Получаю ошибку `sh: 1: vk-tunnel: Permission denied`. Что делать?

Если вы выполнили команду `npm run tunnel` на Linux и видите ошибку доступа, выдайте права на выполнение исполняемому файлу библиотеки:

```Командная&nbsp;строка
chmod +x <полный-путь-к-папке-проекта>/node_modules/@vkontakte/vk-tunnel/bin/vk-tunnel
```

#### Получаю ошибку `Error: 5: User authorization failed: invalid session`. Что делать?

Если вы выполнили команду `npm run deploy` и видите ошибку авторизации, обновите токен библиотеки.

```Windows
del C:\Users\<имя-пользователя>\.config\configstore\@vkontakte\vk-miniapps-deploy.json
```

```macOS и Linux
rm ~/.config/configstore/@vkontakte/vk-miniapps-deploy.json
```

#### Получаю ошибку `Error: Пользователь не является создателем приложения`. Что делать?

Если вы выполнили команду `npm run tunnel` и видите ошибку `ERROR: Пользователь не является создателем приложения`, обновите токен утилиты:

```Windows
del C:\Users\<имя-пользователя>\.config\configstore\@vkontakte\vk-tunnel.json
```

```macOS и Linux
rm ~/.config/configstore/@vkontakte/vk-tunnel.json
```

> **Обратите внимание!** Если ошибка сохраняется, убедитесь, что вас добавили в [администраторы](mini-apps/settings/managers) мини-приложения.

#### Получаю ошибку `Error: connect ECONNREFUSED 127.0.0.1:10888`. Что делать?

Убедитесь, что вы запустили проект:

```Командная&nbsp;строка
npm run start -- --host
```

#### Какие библиотеки используются для создания мини-приложений?

Обязательна к использованию только одна библиотека — [VK Bridge](bridge/getting-started). Она необходима для взаимодействия с площадкой ВКонтакте и Одноклассники.

Ещё вы можете использовать следующие необязательные библиотеки:

* [VKUI](libraries/vkui) — библиотека React-компонентов, которая помогает создавать UI в стиле ВКонтакте.

* [icons](https://vkcom.github.io/icons/) — библиотека SVG-иконок. Обычно используется вместе с VKUI.

* [VK Tunnel](libraries/tunnel) — создаёт специальное соединение (туннель) к веб-серверам, которые работают на локальном компьютере.

* [vk-miniapps-deploy](mini-apps/development/hosting/overview) — используется для размещения файлов приложения на хостинге.

* [vk-mini-apps-router](libraries/router) — библиотека для маршрутизации и навигации в VKUI-приложениях.

#### Включена ли поддержка VK Bridge в VKUI?

Приложение, созданное с помощью пакета create-vk-mini-app, использует [VKUI](libraries/vkui) — библиотеку React-компонентов для создания UI мини-приложений.

Ранние версии VKUI включали поддержку библиотеку [VK Bridge](bridge/overview), которая необходима для работы мини-приложений. Она устанавливалась на компьютеры разработчиков при установке пакета VKUI.

По мере своего развития VKUI превратилась в полноценную React-библиотеку, которую можно использовать не только для создания мини-приложений ВКонтакте, но и для разработки других приложений на React. Поэтому при подготовке VKUI версии 6.0 разработчики приняли решение исключить из неё поддержку VK Bridge.

Шаблоны в create-vk-mini-app настроены на использование VKUI версии 4.28. Эта версия включает поддержку VK Bridge. Поэтому мини-приложения, созданные с помощью create-vk-mini-app, работают корректно.

Если вы будете подключать VKUI самостоятельно, то скорее всего вы будете использовать последнюю версию этой библиотеки — 6.0 или более позднюю, в которой поддержка VK Bridge отсутствует. В этом случае вам надо самостоятельно подключить VK Bridge к своему проекту. Это несложно, подробные инструкции — в [документации VKUI](https://vkui.io/integrations/vk-mini-apps).

#### Как подключить стили CSS в мини-приложении?

Подключите стили в файле `index.js`:

```JavaScript
import './style.css';
```

Если стили подключены неправильно, в мини-приложении отобразится пустая страница.

#### Как поделиться мини-приложением с друзьями?

По умолчанию мини-приложение отключено для пользователей и видно только его создателю по ссылке `vk.com/app{ID}` ВКонтакте и `ok.ru/app/{ID}`.

Чтобы открыть мини-приложение конкретному пользователю ВКонтакте, добавьте пользователя в [администраторы мини-приложения](mini-apps/settings/managers#Добавление%20администратора). Вы можете сделать его администратором с частичным доступом с правом «Тестирование». Это даст пользователю возможность работать с отключённым приложением.

Ещё возможный вариант: создать в настройках, в разделе [Тестирование](mini-apps/settings/test-groups) группу тестировщиков и включить пользователя в эту группу.

Чтобы открыть мини-приложение всем, зайдите в [Настройки &rarr; Размещение](mini-apps/settings/general/placement#Состояние%20для%20пользователей). Измените **Состояние для пользователей** мини-приложения на **Включено**.

В Одноклассниках приложение будет доступно другим пользователям только после прохождения [модерации](mini-apps/settings/moderation).

#### Как запустить мини-приложение в мобильной версии сайта

Мини-приложение можно запустить в мобильной версии сайта по прямой ссылке.

1. Укажите URL для запуска мини-приложения в настройке [Настройки &rarr; Размещение &rarr; Мобильная версия сайта &rarr; URL](mini-apps/settings/general/placement#URL).

1. Откройте в мобильном браузере ссылку на мини-приложение:

    * `https://m.vk.com/app{ID}` для ВКонтакте.

    * `https://m.ok.ru/app/{ID}` для Одноклассников.

    где `{ID}` — идентификатор мини-приложения, который отображается в [настройках](mini-apps/settings/overview#Где%20найти%20ID%20мини-приложения).

#### Как запустить мини-приложение в мобильном приложении ВКонтакте

1. Укажите URL для запуска мини-приложения в настройке [Настройки &rarr; Размещение &rarr; Мобильное приложение &rarr; URL](mini-apps/settings/general/placement).

1. Откройте мобильное приложение и перейдите в сообщения.

1. Отправьте в чат **Избранное** сообщение со ссылкой на мини-приложение:

        * `https://vk.com/app{ID}` для ВКонтакте.

        * `https://ok.ru/app/{ID}` для Одноклассников.

        где `{ID}` — [идентификатор мини-приложения](mini-apps/settings/overview#ID) из настроек.

1. Нажмите на ссылку в отправленном сообщении. Мобильное приложение запустит мини-приложение в элементе управления WebView.

#### Как сделать работу мини-приложения корректной на обоих доменах ВКонтакте — vk.com и vk.ru?

Некоторые разработчики проверяют входящие запросы и указывают в конфигурации мини-приложения адреса надёжных сайтов. Не забудьте добавить домен vk.ru в список доверенных сайтов. Это нужно, чтобы ваше мини-приложение работало корректно у пользователей, которые запустили его на домене vk.ru.

Если в своих мини-приложениях при отправке ответов с сервера вы используете заголовок [X-Frame-Options](https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/X-Frame-Options), то рекомендуем внести правки, чтобы поддержать значения `vk.ru` и `m.vk.ru`.

#### У меня возникли проблемы при разработке мини-приложения в режиме инкогнито

Из-за настроек безопасности некоторые браузеры блокируют файлы cookie и объект `localStorage` в режиме инкогнито. Это может приводить к ошибкам в работе мини-приложения. Чтобы обойти ограничения, используйте специальные события библиотеки VK Bridge для хранения данных:

* [`VKWebAppStorageSet`](bridge/VKWebAppStorageSet)

* [`VKWebAppStorageGet`](bridge/VKWebAppStorageGet)

* [`VKWebAppStorageGetKeys`](bridge/VKWebAppStorageGetKeys)
