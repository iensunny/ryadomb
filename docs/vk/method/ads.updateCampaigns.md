# ads.updateCampaigns

> Источник: [https://dev.vk.ru/ru/method/ads.updateCampaigns](https://dev.vk.ru/ru/method/ads.updateCampaigns)
Редактирует рекламные кампании.

## Параметры

| Параметр | Тип | Обязательный | Описание |
|---|---|---|---|
| `account_id` | `integer` | да | Идентификатор рекламного кабинета. |
| `data` | `text` | да | Сериализованный JSON-массив объектов, описывающих изменения в кампаниях. Описание объектов `campaign_mod_specification` см. ниже. |

## `campaign_mod_specification`

`campaign_id`
`integer`, обязательный
ID редактируемой кампании.

`name`
`string`, от 3 до 60 символов 
Название рекламной кампании.

`day_limit`
`integer`
Дневной лимит в рублях.

`all_limit`
`integer` 
общий лимит в рублях.

`start_time`
`integer`
Время запуска кампании в формате `Unixtime`. Укажите `0` для сброса времени запуска.

`stop_time`
`integer` 
Время остановки кампании в формате `Unixtime`. Укажите `0` для сброса времени остановки.

`status`
`integer`, `[0,1]`
Статус рекламной кампании (`1` — запущена, `0` — остановлена).

## Результат

Возвращает массив ответов на каждый запрос в массиве `data`. Соответствующий объект в выходном массиве содержит идентификатор изменяемого клиента и, в случае возникновения ошибки, поля `error_code` и `error_desc`.

## Права доступа

`ads`

## Ошибки

- 602
- 601
