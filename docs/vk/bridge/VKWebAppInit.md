# VK Bridge | VKWebAppInit

> Источник: [https://dev.vk.ru/ru/bridge/VKWebAppInit](https://dev.vk.ru/ru/bridge/VKWebAppInit)
<!-- ---
title: 'VK Bridge | Служебные | VKWebAppInit'
is_hidden: false
is_search_available: true
menu: 'main_menu'
visible_to_search_robots: true
meta_description: 
redirect_to:
lang: ru
--- -->

# VKWebAppInit

`VKWebAppInit` — первое событие, которое ваше приложение должно отправить приложению для начала работы с VK Bridge. Если это событие не вызвать, ваше мини-приложение или игра не будет работать.

:::note
**Обратите внимание!** На платформах Web и Mobile Web [обрабатывается](#Особенности%20использования) только первый вызов события `VKWebAppInit`. 
:::

## Пример

```JavaScript
bridge.send('VKWebAppInit')
  .then((data) => { 
    if (data.result) {
      // Приложение инициализировано
    } else {
      // Ошибка
    }
  })
  .catch((error) => {
    // Ошибка
    console.log(error);
  });
```

## Совместимость

| Площадки | Платформы |
| --- | --- |
| ВКонтакте | Android, iOS, Mobile Web, Web |
| Одноклассники | Android, iOS, Mobile Web, Web |

## Параметры

—

## Результат

Проверить результат можно:

* Используя объект [`Promise`](#Объект%20Promise), который возвращается вызовом `bridge.send(...)`.

* С помощью [событий](#События) `VKWebAppInitResult` и `VKWebAppInitFailed`.

[Подробнее о проверке результатов при вызовах VK Bridge](bridge/getting-started#Обработка%20результата).

### Объект `Promise`

Если обращение к платформе прошло успешно, управление будет передано в `then`-обработчик объекта `Promise`. В качестве ответа платформа возвращает объект со следующим полем:

| Поле | Тип | Описание |
| --- | --- | --- |
| `result` | `boolean` | `true`, если приложение инициализировано.&#x0d;&#x0a;`false` — в ином случае. |

Если при обращении к платформе произошла ошибка, управление передаётся в метод `catch`. В качестве ответа платформа возвращает [объект с информацией об ошибке](bridge/getting-started#Обработка%20ошибок), общий для всех событий VK Bridge.

### События

#### `VKWebAppInitResult`

Сигнализирует, что приложение инициализировано. В обработчик события на стороне пользователя передаются следующие данные:  

```JavaScript
{
  detail: {
    type: "VKWebAppInitResult",
    data: {
      result: true
    }
  }
}
```

Передаваемый объект подобен объекту, возвращаемому при [успешном выполнении промиса](#Объект%20Promise).

#### `VKWebAppInitFailed`

Информирует об ошибке, которая произошла при взаимодействии с платформой.

В обработчик события на стороне пользователя передаётся [объект с информацией об ошибке](bridge/getting-started#Обработка%20ошибок), общий для всех методов VK Bridge.

#### Пример обработки событий

Подробнее — в разделе [Обработка результата](bridge/getting-started#Обработка%20результата).

## Особенности использования

С 25 сентября 2023 года на платформах Web и Mobile Web обрабатывается только первый вызов события `VKWebAppInit`. Ранее платформы обрабатывали и повторный вызов события.

Эффекты, которые возникают после вызова `VKWebAppInit`:

* Платформа отправляет приложению событие [`VKWebAppUpdateConfig`](bridge/VKWebAppUpdateConfig).
* Отображаются всплывающие подсказки, по которым пользователь сможет ориентироваться в приложении.
* При запуске приложения в мобильном браузере фокус перемещается в приложение.

Ранее вы могли использовать эти эффекты при повторных вызовах `VKWebAppInit`. Так как теперь повторные вызовы не обрабатываются, то и эти эффекты не возникают. Имейте это в виду при разработке ваших приложений.

## Материалы по теме

* [Мини-приложения. Первые шаги](mini-apps/getting-started)
* [Игры. Первые шаги](games/getting-started)
* [Событие VKWebAppUpdateConfig](bridge/VKWebAppUpdateConfig)
