# VK Bridge | VKWebAppAddToChat

> Источник: [https://dev.vk.ru/ru/bridge/VKWebAppAddToChat](https://dev.vk.ru/ru/bridge/VKWebAppAddToChat)
<!-- ---
title: 'VK Bridge | Социальные механики и взаимодействие | VKWebAppAddToChat'
is_hidden: false
is_search_available: true
menu: 'main_menu'
type: 'page'
visible_to_search_robots: true
meta_description: 
redirect_to:
lang: ru
--- -->

# VKWebAppAddToChat

:::note
**Важно!** Событие `VKWebAppAddToChat` больше не поддерживается.
:::

`VKWebAppAddToChat` добавляет [виджет](mini-apps/development/integration-into-messenger/overview) мини-приложения в чат.

Доступны два сценария работы события:
* Если мини-приложение было открыто из аттач-пикера (элемента добавления вложения) в чате, то будет показано диалоговое окно с подтверждением. После подтверждения виджет будет добавлен в чат.
* Если мини-приложение было открыто другим способом, то сначала будет показано диалоговое окно с подтверждением, а затем окно с выбором чата для отправки виджета. После этого виджет будет добавлен в выбранный чат.

:::carousel
![alt=Добавление виджета из аттач-пикера;title=Добавление виджета из аттач-пикера](1d126e35b035912fb7c5d003f6be109c32f183ed7a57502f094297f6 "8183004204365359421")
![alt=Виджет отправлен в чат;title=Виджет отправлен в чат](93e572db574dbfd41763ae77f265f7b90afb24d5521775f2d11b926e "-5228390028034567396")
:::

## Пример

```JavaScript
bridge.send("VKWebAppAddToChat", {
    action_title: 'Показать погоду'
});
```

## Совместимость

| Площадки | Платформы |
| --- | --- |
| ВКонтакте | Android, iOS, Mobile Web, Web |
| Одноклассники | – |

## Параметры

| Поле | Тип | Описание |
| --- | --- | --- |
| `action_title` &#x0d;&#x0a;*обязательное* | `string` | Заголовок действия для виджета. |
| `close_app` &#x0d;&#x0a;*необязательное* | `boolean` |&nbsp;&nbsp; &bullet; `true` — закрыть мини-приложение после отправки виджета в чат.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `false` — показать снек-бар с кнопкой перехода в чат и не закрывать мини-приложение. &#x0d;&#x0a; Параметр применяется только для случая, когда мини-приложение было открыто из аттач-пикера. Значение по умолчанию — `false`. |
| `hash` &#x0d;&#x0a;*необязательное* | `string` | Произвольная строка, содержащая хеш мини-приложения, который будет добавляться к URL после символа `#` |

## Результат

Проверить результат можно:

* Используя объект [`Promise`](#Объект%20Promise), который возвращается вызовом `bridge.send(...)`.

* С помощью [событий](#События) `VKWebAppAddToChatResult` и `VKWebAppAddToChatFailed`.

[Подробнее о проверке результатов при вызовах VK Bridge](bridge/getting-started#Обработка%20результата).

### Объект `Promise`

Если обращение к платформе прошло успешно, управление будет передано в `then`-обработчик объекта `Promise`. В качестве ответа платформа возвращает объект со следующим полем:

| Поле | Тип | Описание |
| --- | --- | --- |
| `result` | `boolean` | `true`, виджет отправлен в чат. |

Если при обращении к платформе произошла ошибка, управление передаётся в метод `catch`. В качестве ответа платформа возвращает [объект с информацией об ошибке](bridge/getting-started#Обработка%20ошибок), общий для всех событий VK Bridge.

### События

#### `VKWebAppAddToChatResult`

Сигнализирует, что виджет добавлен в чат. В обработчик события на стороне пользователя передаются следующие данные:  

```JavaScript
{
  detail: {
    type: "VKWebAppAddToChatResult",
    data: {
      result: true
    }
  }
}
```

Передаваемый объект подобен объекту, возвращаемому при [успешном выполнении промиса](#Объект%20Promise).

#### `VKWebAppAddToChatFailed`

Информирует об ошибке, которая произошла при взаимодействии с платформой.

В обработчик события на стороне пользователя передаётся [объект с информацией об ошибке](bridge/getting-started#Обработка%20ошибок), общий для всех методов VK Bridge.

#### Пример обработки событий

Подробнее — в разделе [Обработка результата](bridge/getting-started#Обработка%20результата).

## Примечания

* Чтобы сообщить мини-приложению, что оно запущено из чата, ВКонтакте передаёт ему URL-параметр `vk_chat_id`, который содержит идентификатор чата.
* Отличить, откуда запущено мини-приложение, можно с помощью параметра `vk_ref`:
    * `vk_ref=im_attach_picker` – запуск из аттач-пикера.
    * `vk_ref=catalog_messenger_apps_with_action` – запуск из каталога.
    * `vk_ref=im_app_action` – запуск из отправленного виджета.

## Материалы по теме

* [Параметры запуска мини-приложений](mini-apps/development/launch-params)

* [Значения параметра `vk_ref` для мини-приложений](mini-apps/development/launch-params/vk_ref)
