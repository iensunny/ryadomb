# Поиск мест интереса

> Источник: [https://dev.vk.ru/ru/vkmaps/search-and-geocoding/places](https://dev.vk.ru/ru/vkmaps/search-and-geocoding/places)
**Поиск мест интереса** — это сервис, который позволяет осуществлять поиск мест интереса и дополнительной информации о них.

:::note
`/places` — точка вызова сервиса поиска мест интереса и получения дополнительной информации о них.
:::

## Запрос

### Обязательные параметры запроса

| Имя поля | Формат | Описание | Пример |
| --- | --- | --- | --- |
| `api_key` | `hex-string` | [Доступ к сервисам](vkmaps/general-information/api-key) | `api_key=fa749bace6d8a3b1....` |
| `q` | `string` | Тело поискового запроса. В запросе можно использовать один из варинтов для поиска: &#x0d;&#x0a;  &bull; координаты в формате `lat,lon`, где: `lat` — широта искомой точки в градусах (используется 6 знаков после запятой); `lon` — долгота искомой точки в градусах (используется 6 знаков после запятой);&#x0d;&#x0a;  &bull; текстовая строка (может содержать имя искомого места интереса или адрес). | `q=Mail.ru`; `q=метро`; `q=55.797041,37.537830`; `q=210000000FE79787` |

### Дополнительные параметры запроса

| Имя поля | Формат | Описание | Пример |
| --- | --- | --- | --- |
| `fields` | `fieldname1,`&#x0d;&#x0a;`fieldname2,...`&#x0d;&#x0a;`fieldnameN` | Выбор полей, отображаемых в ответе. Возможные значения:&#x0d;&#x0a;  &bull; `name` (по умолчанию) — название места интереса;&#x0d;&#x0a;  &bull; `place_details` — детальная информация о месте интереса (на текущий момент не поддерживается);&#x0d;&#x0a;  &bull; `address_details` (по умолчанию) — детальная информация о найденном адресе с почтовым индексом без координат;&#x0d;&#x0a;  &bull; `address` — адрес найденного места интереса одной строкой;&#x0d;&#x0a;  &bull; `pin` (по умолчанию) — координаты найденного объекта (долгота и широта);&#x0d;&#x0a;  &bull; `bbox` — область местонахождения объекта;&#x0d;&#x0a;  &bull; `geometry` — границы объекта;&#x0d;&#x0a;  &bull; `type` —  тип объекта. | `fields=pin,bbox,name` |
| `types` | `string` | Тип объектов, по которым осуществляется поиск. По умолчанию поиск осуществляется по всем типам мест интересов. Справочник постоянно актуализируется. Для получения полного и актуального справочника типов можно воспользоваться сервисом `/types`. | `types=hotel,metro` |
| `lang` | 2-х&#x0d;&#x0a;символьный&#x0d;&#x0a;код языка | Язык ответа на одном из [доступных языков](vkmaps/general-information/general#Поддерживаемые%20языки). По умолчанию на языке региона, в котором найден объект. | `lang=en` |
| `location` | `string` | Координата, относительно которой будет осуществляться поиск и ранжирование результатов ответа. Координаты в формате `lat,lon`, где:&#x0d;&#x0a;  &bull; `lat` — широта искомой точки в градусах (используется 6 знаков после запятой);&#x0d;&#x0a;  &bull; `lon` — долгота искомой точки в градусах (используется 6 знаков после запятой). Результаты поиска будут отсортированы по расстоянию от указанной координаты. | `location=55.796743,37.537354` |
| `radius` | `integer` | Радиус поиска в метрах относительно параметра `location`. Параметр `location` при использовании параметра `radius` является обязательным. В случае использования малого радиуса объект может быть не найден. | `radius=500` |
| `limit` | `integer` | Ограничение числа объектов в ответе. поддерживаются значения в интервале от `1` до `100`. По умолчанию — `5`. В случае использования больших значений, длительность получения ответа может увеличиться | `limit=10` |
| `isocode` | `2char` | Двухбуквенный код страны согласно ISO 3166-1 alpha-2 | `isocode=RU` |

## Ответ

Ответ предоставляется только в JSON формате и содержит информацию, найденную по запросу.

### JSON формат

| Имя поля | Формат | Описание |
| --- | --- | --- |
| `request` | `string` | Запрос. |
| `results` | `list` | Найденный результат. |
| `address` | `string` | Полный адрес найденного объека. |
| `address_details` | `list` | Детальная информация об адресе найденного объекта. |
| `pin` | `list` | Координаты найденного объекта (долгота и широта). |
| `bbox` | `list` | Границы местонахождения найденного объекта для позиционирования на карте. |
| `geometry` | `list` | Геометрия найденного объекта объекта. |
| `name` | `string` | Название найденного объекта. |
| `type` | `string` | Тип найденного объекта. |

### Описание полей address_details

| Имя поля | Формат | Описание |
| --- | --- | --- |
| `country` | `string` | Страна. |
| `isocode` | `2char` | Двухбуквенный код страны согласно ISO 3166-1 alpha-2. |
| `region` | `string` | Область. |
| `subregion` | `string` | Район |
| `locality` | `string` | Населённый пункт. |
| `sublocality` | `string` | Микрорайон или название жилого комплекса. |
| `street` | `string` | Улица. |
| `building` | `string` | Номер дома, части дома или строения. |
| `suburb` | `string` | Район или микрорайон населённого пункта. |
| `postal_code` | `string` | Почтовый код. |

### Описание полей geometry

| Имя поля | Формат | Описание |
| --- | --- | --- |
| `type` | `string` | Тип геометрии:&#x0d;&#x0a;  &bull; `Point` — точка;&#x0d;&#x0a;  &bull; `MultiPoint` — несколько точек;&#x0d;&#x0a;  &bull; `Linestring` — линия;&#x0d;&#x0a;  &bull; `MultiLineString` — несколько линий;&#x0d;&#x0a;  &bull; `Polygon` — полигон;&#x0d;&#x0a;  &bull; `MultiPoligon` — несколько полигонов. |
| `coordinates` | `list` | Массив точек (долгота и широта) описывающий геометрию объекта. |

## Пример

### Пример запроса

[https://demo.maps.vk.com/api/places?api_key=test&q=prime&fields=name,place_details,address,address_details,pin,bbox,geometry,type&location=55.7967432,37.5373542&limit=1](https://demo.maps.vk.com/api/places?api_key=test&q=prime&fields=name,place_details,address,address_details,pin,bbox,geometry,type&location=55.7967432,37.5373542&limit=1)

### Пример ответа

```
{
    "request": "/v3/places?location=55.7967432,37.5373542&api_key=internal_&q=prime&fields=name,place_details,address,address_details,pin,bbox,geometry,type",
    "results":
    [
        {
            "address": "Россия, Москва, Северный административный округ, Москва, район Аэропорт, Ленинградский проспект, 72 к4",
            "address_details":
            {
                "building": "72 к4",
                "country": "Россия",
                "isocode": "RU",
                "locality": "Москва",
                "region": "Москва",
                "street": "Ленинградский проспект",
                "subregion": "Северный административный округ",
                "suburb": "район Аэропорт"
            },
            "bbox": null,
            "geometry":
            {
                "coordinates":
                [
                    37.519963,
                    55.805396
                ],
                "type": "Point"
            },
            "name": "Прайм",
            "pin":
            [
                37.519963,
                55.805396
            ],
            "type": "food/cafe"
        }
    ]
}
```
