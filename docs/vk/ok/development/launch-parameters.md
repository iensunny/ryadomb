# Одноклассники | Разработка | Параметры запуска

> Источник: [https://dev.vk.ru/ru/ok/development/launch-parameters](https://dev.vk.ru/ru/ok/development/launch-parameters)
<!--
title: 'Одноклассники | Разработка | Параметры запуска'
is_hidden: false
is_search_available: true
type: 'page'
menu: 'main_menu'
visible_to_search_robots: true
meta_description: 
redirect_to:
lang: ru
--- -->

# Параметры запуска в Одноклассниках

При старте приложения в Одноклассниках платформа передаёт ему дополнительные параметры с информацией о пользователе и источнике запуска. Значения передаются как URL-параметры по адресу, указанному в настройках игры или приложения.

Чтобы передать параметры, укажите их в URL после символа `?`, например `https://my-app.com/start?param1=value1`.

Чтобы получить параметры запуска в коде приложения на стороне пользователя, воспользуйтесь одним из способов:

* Извлеките значение свойства `href` объекта `window.location`.
* Вызовите событие [`VKWebAppGetLaunchParams`](bridge/VKWebAppGetLaunchParams).

## Подпись параметров

Чтобы защититься от подмены значений возможной утечки данных, используйте подпись параметров. Подробности — в разделе [Подпись параметров запуска](mini-apps/development/launch-params-sign).

## Параметры

| Параметр | Тип | Описание |
| --- | --- |  --- |
| `sign` | `string` | base64-строка — подпись параметров запуска. Используется для обнаружения подмены параметров третьими лицами. Подробнее — в разделе [Подпись параметров запуска](mini-apps/development/launch-params-sign).|
| `vk_access_token_settings` | `string` | Список прав доступа пользователя, который запустил приложение. Каждое значение в списке соответствует области данных пользователя, к которой можно получить доступ. Значения в списке разделены запятыми, например `phone_number,email,geo_data`. Полный список разрешений — в документации Одноклассников в разделе [Разрешения для приложения](https://apiok.ru/dev/types/enums.ApplicationPermission). |
| `vk_app_id` | `integer` | Идентификатор связанного приложения ВКонтакте. |
| `vk_are_pushes_enabled` | `integer` | Включены ли у пользователя оповещения от приложения ВКонтакте. | 
| `vk_ok_app_id` | `integer` | Идентификатор приложения в Одноклассниках. |
| `vk_are_notifications_enabled` | `integer` | Информация о разрешённых уведомлениях от приложения:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — отправка уведомлений запрещена.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — отправка уведомлений разрешена. |
| `vk_client` | `string` | Идентификатор площадки запуска приложения. Для Одноклассников всегда `ok`. | 
| `vk_is_app_user` | `integer` | Информация о том, установил ли пользователь приложение :&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — приложение не установлено.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — приложение установлено.&#x0d;&#x0a;&#x0d;&#x0a;После того как пользователь впервые запустил приложение, они считаются установленными.|
| `vk_language` | `string` | Язык пользовательского интерфейса приложения. |
| `vk_platform` | `string` | Платформа, на которой было запущено приложение:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `desktop_web_ok` — десктопная версия сайта Одноклассники (ok.ru).&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_android_ok` — мобильное приложение Одноклассники для Android.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_iphone_ok` — мобильное приложение Одноклассники для iOS.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `mobile_web_ok` — мобильная версия сайта Одноклассники (m.ok.ru).|
| `vk_ref` | `string` | Источник запуска приложения ВКонтакте. Для игр и мини-приложений есть свой возможный список значений:&#x0d;&#x0a;&nbsp;&nbsp; &bullet; [Список значений для игр](games/development/parameters/vk_ref)&#x0d;&#x0a;&nbsp;&nbsp; &bullet; [Список значений для мини-приложений](mini-apps/development/launch-params/vk_ref) |
| `vk_ok_ref` | `string` | Источник запуска приложения в Одноклассниках. Полный список значений — в документации Одноклассников в разделе [Список возможных мест перехода в игру](https://apiok.ru/dev/types/enums.ApplicationReferencePlace). |
| `vk_ts` | `integer` | Время генерации подписи параметров запуска. Подпись передаётся в параметре `sign`.|
| `vk_user_id`| `integer` | VK ID пользователя, который запустил приложение. Если приложение открыл пользователь Одноклассников, у которого не привязан VK ID, в поле записывается технический идентификатор. Когда пользователь свяжет аккаунты, технический идентификатор сохранится в поле `vk_original_vk_id`, а в `vk_user_id` запишется его действительный VK ID. |
| `vk_ok_user_id` | `integer` | Идентификатор пользователя Одноклассников, который запустил приложение. |
| `vk_original_vk_id` | `integer` | Исходный VK ID пользователя. Может отличаться от `vk_user_id`, если изначально был присвоен технический идентификатор. |

:::note
Параметры `vk_user_id`, `vk_ok_user_id`, `vk_original_vk_id` используются для авторизации пользователя в приложении. Чтобы проверить подлинность полученных данных, вычислите [подпись](mini-apps/development/launch-params-sign) и сравните с полученным значением параметра `sign`.
:::

<!-- то, что ниже — параметры ВК. они не были перечислены в https://confluence.vk.team/pages/viewpage.action?pageId=1677782768. оставила на всякий случай -->

<!--| `vk_chat_id` | `string` | Идентификатор чата. Передаётся, если приложение было запущено из чата. |-->
<!--| `vk_group_id` | `integer` | Идентификатор сообщества, в котором установлено приложение. Передаётся, если приложение:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Запущено с помощью кнопок в сообществе или из блока приложений сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Запущено по ссылке вида: `vk.com/app6909581_-166562603`.&#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|-->
<!--| `vk_is_favorite` | `integer` | Информация о добавлении приложения в избранное:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — приложение не добавлено в избранное.`1` — приложение добавлено в избранное.|-->
<!--| `vk_is_play_machine` | `integer` | `1`, если игра была запущена через [Play Machine](games/faq#Поддерживаются%20ли%20Flash-игры?) (специальное приложение для запуска Flash-игр). В ином случае параметр равен `0` или не передаётся. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в приложениях.|-->
<!--| `vk_is_recommended` | `integer` | Информация о том, [рекомендовал](mini-apps/promotion/user-recommendation) ли пользователь, указанный в параметре `vk_user_id`, приложение своим друзьям:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `0` — приложение не порекомендовано.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `1` — приложение порекомендовано.&#x0d;&#x0a;&#x0d;&#x0a;Если параметр не передан, значит, что приложение ещё не порекомендовано.&#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|-->
<!--| `vk_is_widescreen` | `integer` | `1`, если игра запущена в [широкоформатном режиме](games/settings/general/display#Широкоформатный%20режим), иначе — `0`. Параметр используется только для [Web-игр](games/getting-started#Direct%20Games%20и%20Web-игры). &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в приложениях.|-->
<!--| `vk_has_profile_button` | `integer` | Информация о том, установил ли пользователь кнопку в профиле, по которой запускается приложение. Значение 1 означает, что пользователь установил кнопку в профиле, иначе параметр не передаётся. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|-->
<!--| `vk_request_key` | `string` | Используется при старте игры из [запроса о помощи](games/promotion/game-mechanics/requests) или [приглашения](games/promotion/game-mechanics/invites) друга. Указывает ключ запроса или приглашения. Это то же самое значение, которое было передано в параметре `requestKey` при вызове события [`VKWebAppShowRequestBox`](bridge/VKWebAppShowRequestBox) или [`VKWebAppShowInviteBox`](bridge/VKWebAppShowInviteBox) библиотеки [VK Bridge](bridge/overview).&#x0d;&#x0a;&#x0d;&#x0a;Используйте это значение в игре, чтобы предусмотреть сценарий, соответствующий запросу или приглашению, а также для отслеживания конверсии. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в приложениях. |-->
<!--| `vk_profile_id` | `integer` | Идентификатор пользователя, по кнопке в профиле которого запускается приложение. Если приложение запущено не по кнопке в профиле, параметр не передаётся. &#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх.|-->
<!--| `vk_testing_group_id` | `integer` | Идентификатор тестовой группы, в которой состоит пользователь. Если пользователь не состоит в тестовой группе, параметр не передаётся.|-->
<!--| `vk_viewer_group_role` | `string` | Роль пользователя в сообществе, из которого запущено приложение:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `admin` — администратор или создатель сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `editor` — редактор сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `member` — подписчик сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `moder` — модератор сообщества.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; `none` — не состоит в сообществе.&#x0d;&#x0a;&#x0d;&#x0a;Информация о сообществе возвращается, если:&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Мини-приложение запущено с помощью кнопок в сообществе.&#x0d;&#x0a; &nbsp;&nbsp; &bullet; Исходная ссылка на приложение имела вид: `vk.com/app6909581_-166562603`.&#x0d;&#x0a;&#x0d;&#x0a;Параметр не используется в играх. |-->

## Передача произвольных параметров

При необходимости вы можете передавать произвольные параметры в приложение.

#### Запуск по прямой ссылке

При запуске приложения по прямой ссылке укажите параметры после символа `#` в адресной строке, например:

```
https://vk.com/app1234567#param1=12345
```

Символы после `#` будут переданы приложению в URL-параметре `hash`. Этот параметр всегда идёт последним среди остальных передаваемых параметров. Все символы после него являются символами, которые были указаны после `#` в URL приложения.  

Получить параметры можно и с помощью свойства `hash` объекта `window.location`. Для отслеживания изменений этого значения в приложении используйте события [`VKWebAppLocationChanged`](bridge/VKWebAppLocationChanged) и [`VKWebAppChangeFragment`](bridge/VKWebAppChangeFragment) библиотеки [VK Bridge](bridge/overview).  

Использование знака вопроса (`?`) для передачи параметров при запуске по прямой ссылке не работает.

#### Параметры запуска в настройках приложения

Чтобы указать параметры запуска в настройках приложения, добавьте в URL знак вопроса и параметры, например:

```
https://my-game.example.com/start-page?param1=12345
```

Получить эти параметры вы можете так же, как и обычные параметры веб-страниц, например с помощью свойства `window.location.search` или с помощью события [`VKWebAppGetLaunchParams`](bridge/VKWebAppGetLaunchParams).

Использовать символы `#` в URL-адресах, которые указываются в настройках приложения, нельзя.

## Материалы по теме

* [Подпись параметров запуска](mini-apps/development/launch-params-sign)
* [`VKWebAppGetLaunchParams`](bridge/VKWebAppGetLaunchParams)
